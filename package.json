{
  "name": "backendnew",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../reactpractice/practice && npm run build && cp -r dist ../backendNew",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "devin beu",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^4.21.2"
  }
}
